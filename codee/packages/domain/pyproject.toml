[project]
name = "leltariv-domain"
version = "0.1.0"
description = "Entitások és üzleti szabályok — külső függőség nélkül, ez a lényege"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
